# Dev container environment for homebranch-web
# This file is committed and auto-copied to .env when the devcontainer is created.
# Edit .env directly if you need to override values for your local setup.

# Where Vite proxies /api/* — homebranch backend running in its own devcontainer
VITE_API_ROOT=http://host.docker.internal:8080

# Where Vite proxies /auth/* — Authentication backend running in its own devcontainer
VITE_AUTHENTICATION_ROOT=http://host.docker.internal:3000

# Vite dev server bind host — allows connections from outside the container
ALLOWED_HOST=localhost

# Proxy URL prefixes (used by Vite config and baked into the bundle)
VITE_API_URL=/api
VITE_AUTHENTICATION_URL=/auth

# Pagination
VITE_ITEMS_PER_PAGE=20
